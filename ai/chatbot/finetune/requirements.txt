# Fine-tuning dependencies — not needed in production Docker image
# Install separately: pip install -r finetune/requirements.txt

sentence-transformers>=2.7.0
torch>=2.2.0
asyncpg==0.29.0
httpx==0.27.0
